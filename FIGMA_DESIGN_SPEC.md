# AntiGrind - 反内卷APP Figma 设计规范与提示词

## 📋 项目概述

**应用名称**：AntiGrind（反内卷）
**应用定位**：透明化的"工时数据库"移动应用
**核心理念**：让消费者通过日常购物决策，共同抵制内卷型公司，推动职场环境改善
**目标平台**：iOS / Android（React Native + Expo）
**设计风格**：现代简约、信息密度适中、数据可视化导向

---

## 🎯 目标用户群体特征

### 主要用户画像
- **年龄层**：22-35岁（Z世代及千禧一代）
- **职业特征**：互联网从业者、白领、应届毕业生
- **价值观**：
  - 关注工作生活平衡（Work-Life Balance）
  - 具有社会责任感和消费者意识
  - 对企业用工透明度有强烈需求
  - 倾向于用消费投票表达立场

### 用户使用场景
1. **购物决策时刻**：在超市/电商平台扫码查看产品背后的公司用工情况
2. **求职参考**：搜索意向公司的工时数据和员工评价
3. **数据贡献者**：主动提交所在公司的真实工时数据
4. **社区互动**：浏览红黑榜、参与讨论、获取积分奖励

---

## 🎨 设计系统规范

### 1. 色彩方案（Color Palette）

#### 主色调（Primary Colors）
```
品牌蓝（Brand Blue）: #3b82f6
- 用于：主要按钮、激活状态、链接文字、重要操作
- 应用场景：登录按钮、提交按钮、底部导航栏激活态

深蓝（Dark Blue）: #2563eb
- 用于：按钮按下状态、hover效果
```

#### 功能色（Semantic Colors）
```
✅ 绿色系（员工友好 - AGI ≤30分）
- 渐变起始: #22c55e
- 渐变结束: #16a34a
- 含义：低内卷、工作环境友好
- 使用场景：AGI评分徽章、推荐标签

⚠️ 黄色系（一般 - AGI 31-60分）
- 渐变起始: #eab308
- 渐变结束: #ca8a04
- 含义：中等内卷程度
- 使用场景：AGI评分徽章、警示标签

🔴 红色系（内卷严重 - AGI >60分）
- 渐变起始: #ef4444
- 渐变结束: #dc2626
- 含义：高度内卷、需警惕
- 使用场景：AGI评分徽章、避雷提醒

灰色系（暂无数据）
- 渐变起始: #6b7280
- 渐变结束: #4b5563
- 含义：数据不足
- 使用场景：未知状态的徽章
```

#### 中性色（Neutral Colors）
```
背景色:
- 页面背景: #f9fafb（浅灰）
- 卡片背景: #ffffff（纯白）
- 输入框背景: #f3f4f6
- 次级卡片: #f9fafb

文字颜色:
- 主要文字: #1f2937（深灰黑）
- 次要文字: #4b5563（中灰）
- 辅助文字/标签: #6b7280（浅灰）
- 占位符文字: #9ca3af

边框/分割线:
- 默认边框: #e5e7eb
- 强调边框: #d1d5db
```

#### 特殊强调色
```
认证绿: #10b981
- 用于：企业认证相关按钮、已认证状态标识

警告黄: #fef3c7（背景）/ #92400e（文字）
- 用于：代工产品标签、特殊提示

危险红: #ef4444
- 用于：退出登录、删除操作等破坏性动作
```

### 2. 字体排版系统（Typography）

```
字体族: 
- iOS: SF Pro Display / SF Pro Text
- Android: Roboto / Noto Sans CJK
- 回退字体: system-ui, -apple-system, sans-serif

字号层级:
- 大标题（Page Title）: 32px / font-weight: bold / line-height: 1.2
  使用场景：登录页标题
  
- 页面标题（Screen Title）: 24px / font-weight: bold / line-height: 1.3
  使用场景：公司详情页公司名

- 区块标题（Section Title）: 18px / font-weight: 600 / line-height: 1.4
  使用场景：各区块标题（公司简介、工时数据等）

- 正文主体（Body Large）: 16px / font-weight: 600 / line-height: 1.5
  使用场景：公司名称、主要数值

- 正文内容（Body Regular）: 16px / font-weight: normal / line-height: 1.5
  使用场景：描述文本、输入框文字

- 辅助文字（Caption）: 14px / font-weight: normal / line-height: 1.4
  使用场景：次要信息、状态说明

- 标签文字（Label）: 12px / font-weight: 500 / line-height: 1.3
  使用场景：字段标签、小标签

- 徽章文字（Badge Text）: 10-14px / font-weight: 600 / line-height: 1.2
  使用场景：AGI徽章内的分数显示
```

### 3. 间距与布局系统（Spacing & Layout）

```
基础间距单位: 4px
间距阶梯: 4, 8, 12, 16, 20, 24, 32, 40, 48

常用间距规则:
- 页面水平边距: 16-20px
- 卡片内边距: 16-20px
- 区块垂直间距: 12px
- 元素内部间距: 8-12px
- 紧凑元素间距: 4-8px

圆角规范（Border Radius）:
- 小元素（按钮、输入框、小卡片）: 12px
- 中等卡片: 16px
- 大容器: 20-24px
- 圆形元素（头像、图标容器）: 50%（完全圆形）
- 徽章/标签: 16px（胶囊形）

阴影系统（Shadows）:
- 卡片阴影:
  shadowColor: #000
  shadowOffset: { width: 0, height: 2 }
  shadowOpacity: 0.1
  shadowRadius: 8
  elevation: 3 (Android)

- 浮动按钮/弹窗阴影:
  shadowOffset: { width: 0, height: 4 }
  shadowOpacity: 0.15
  shadowRadius: 12
  elevation: 6 (Android)
```

### 4. 图标系统（Iconography）

```
图标库: Ionicons (@expo/vector-icons)
图标风格: 线性图标（Outline Style）
图标尺寸:
- 底部导航栏: 24px（默认size）
- 列表项图标: 20-24px
- 按钮内图标: 18-20px
- 小型指示图标: 16px

主要图标映射:
- 扫描: scan / qr-code-outline
- 搜索: search-outline
- 个人中心: person-outline
- 返回: arrow-back-outline
- 设置: settings-outline
- 提交: add-circle-outline / upload-outline
- 认证: checkmark-circle / shield-checkmark
- 警告: warning-outline
- 信息: information-circle-outline
```

---

## 📱 核心功能模块设计规范

### 模块一：扫码识企业（Scan Screen）

**页面定位**: 首页核心功能入口
**交互流程**:
1. 进入页面 → 显示全屏相机取景框（黑色背景）
2. 对准条形码 → 自动识别并震动反馈
3. 识别成功 → 展示扫描结果卡片（从底部滑入动画）

#### 视觉设计要求：

**相机取景界面**:
- 背景：纯黑 (#000000)
- 取景框：四角有动态扫描线动画（蓝色 #3b82f6）
- 提示文字："将条形码放入框内，自动扫描"
- 位置：居中偏下，白色半透明文字
- 历史记录入口：右上角小图标（时钟图标）

**扫描结果卡片（ScanResultCard）**:
```
容器规格:
- 宽度: 屏幕宽度 - 32px（左右各16px边距）
- 背景: 白色 #ffffff
- 圆角: 16px
- 内边距: 20px
- 阴影: 标准卡片阴影
- 外边距: 16px

内容结构（从上到下）:

┌─────────────────────────────────┐
│  [AGI评分大徽章]                 │ ← 居中，large尺寸
│                                 │
│  产品                            │ ← 标签文字（12px，#6b7280）
│  产品名称/条形码                  │ ← 价值文字（16px，粗体）
│                                 │
│  品牌方 [认证徽章]               │ ← 可点击区域
│  公司名称 [AGI小徽章]             │ ← 右侧对齐
│  [认证状态条]                     │ ← 条件显示
│                                 │
│  制造商 [认证徽章]                │ ← 可点击区域
│  公司名称 [AGI小徽章]             │
│  [认证状态条]                     │
│                                 │
│  [代工产品 黄色标签]              │ ← 条件显示（is_oem）
└─────────────────────────────────┘
```

**AGI评分徽章（AGIBadge）**:
```
三种尺寸:
- Small: 60×24px, 文字10px
  使用场景：列表中的行内徽章
  
- Medium: 80×32px, 文字12px  
  使用场景：一般展示场景
  
- Large: 100×40px, 文字14px
  使用场景：扫描结果顶部突出展示

视觉样式:
- 形状: 胶囊形（border-radius: 16px）
- 背景: 线性渐变（45度角，左上到右下）
- 文字: 白色，600字重，居中
- 内容: "XX分" 或状态文字（员工友好/一般/内卷严重/暂无数据）

渐变色配置:
- Green: [#22c55e → #16a34a]
- Yellow: [#eab308 → #ca8a04]
- Red: [#ef4444 → #dc2626]
- Unknown: [#6b7280 → #4b5563]
```

**交互细节**:
- 点击公司名称 → 跳转至公司详情页
- 长按AGI徽章 → 触发触觉反馈（红色警告/绿色成功）
- 支持下滑关闭结果卡片返回扫描界面

---

### 模块二：企业搜索（Search Screen）

**页面定位**: 主动查询企业信息的入口
**交互流程**:
1. 进入页面 → 搜索框自动聚焦
2. 输入关键词 → 300ms防抖后触发搜索
3. 显示搜索结果列表 → 点击进入详情

#### 视觉设计要求：

**搜索栏区域**:
```
容器:
- 背景: 白色 #ffffff
- 下边框: 1px solid #e5e7eb
- 内边距: 16px

搜索输入框:
- 背景: #f3f4f6
- 圆角: 12px
- 内边距: 12px
- 占位符: "搜索公司名称..."
- 无边框设计
- 右侧可加清除按钮（X图标）
```

**搜索结果列表**:
```
列表容器:
- 背景: #f9fafb
- 内边距: 16px
- 间距: 12px between items

单个公司卡片:
┌──────────────────────────────────────┐
│ 公司名称          [AGI Small Badge]  │
│ 已验证/待验证                        │
└──────────────────────────────────────┘

- 背景: 白色
- 圆角: 12px
- 内边距: 16px
- 行高: 自适应内容
- 左侧：公司信息（名称+验证状态）
- 右侧：AGI评分徽章（small尺寸）

空状态设计:
- 有查询词: "未找到相关公司"（居中，#6b7280）
- 无查询词: "输入关键词搜索公司"（居中，#6b7280）

加载状态:
- 居中显示ActivityIndicator
- 颜色: #3b82f6（品牌蓝）
```

**交互细节**:
- 实时搜索（300ms debounce）
- 支持搜索历史记录显示（最近搜索标签）
- 下拉刷新支持
- 滚动加载更多（如需分页）

---

### 模块三：公司详情页（Company Detail Screen）

**页面定位**: 展示企业完整的工时数据和评价信息
**信息架构**: 从上到下的单列滚动布局

#### 视觉设计要求：

**页面头部区域**:
```
容器:
- 背景: 白色 #ffffff
- 内边距: 20px
- 对齐: 居中
- 底部分割线: 1px solid #e5e7eb

内容结构:
┌──────────────────────────┐
│                          │
│     公司名称（24px粗体）   │
│                          │
│  [AGI Large Badge] [认证] │ ← 水平排列，gap: 12px
│                          │
│  [认证状态进度条]         │ ← 条件显示
│                          │
└──────────────────────────┘
```

**信息区块通用样式**:
```
区块容器:
- 背景: 白色 #ffffff
- 外边距: 上方 12px
- 内边距: 20px
- 圆角: 无（直角）或根据需要设置

区块标题:
- 字号: 18px
- 字重: 600
- 颜色: #1f2937
- 下边距: 12px
```

**公司简介区块**:
```
描述文字:
- 字号: 14px
- 颜色: #4b5563
- 行高: 22px（1.57倍行高）
- 最大行数: 不限制（可滚动）
```

**认证信息区块**（条件显示）:
```
认证卡片:
- 背景: #f9fafb
- 圆角: 12px
- 内边距: 16px

信息行布局（每行）:
┌─────────────────────────────┐
│ 认证状态          已通过     │ ← flex两端对齐
│ 认证时间          2024-01   │
│ 有效期至          2025-01   │
└─────────────────────────────┘

- 标签字号: 14px, #6b7280
- 数值字号: 14px, 500字重, #1f2937
- 行间距: 8px
```

**工时数据区块**:
```
空状态:
- "暂无工时数据"（居中，14px，#6b7280）

单条记录卡片:
- 背景: #f9fafb
- 圆角: 12px
- 内边距: 16px
- 下边距: 12px（多张卡片间）

记录字段（每行）:
┌─────────────────────────────┐
│ 每周工时        45小时      │
│ 休息制度        大小周       │
│ 加班补偿        固定补贴     │
│ 验证状态        待验证(2/5) │
└─────────────────────────────┘

- 标签: 14px, #6b7280
- 数值: 14px, 500字重, #1f2937
- 行间距: 8px
```

**操作按钮区**:
```
主操作按钮（提交工时数据）:
- 背景: #3b82f6（品牌蓝）
- 圆角: 12px
- 内边距: 16px
- 外边距: 20px
- 文字: "提交工时数据", 16px, 600字重, 白色

次级操作按钮（申请认证，条件显示）:
- 背景: #10b981（认证绿）
- 其他样式同上
- 文字: "申请企业认证"
- 与主按钮间距: 0（紧贴）
```

**加载状态**:
```
全屏居中加载:
- ActivityIndicator
- 尺寸: large
- 颜色: #3b82f6

错误状态:
- "公司不存在"（16px, #6b7280, 居中）
```

---

### 模块四：个人中心（Profile Screen）

**页面定位**: 用户账户管理和数据贡献统计
**双态设计**: 未登录态 / 已登录态

#### 未登录态设计：
```
容器:
- 背景: #f9fafb
- 全屏居中布局

内容:
┌────────────────────┐
│                    │
│    请先登录         │ ← 20px, 600字重, #1f2937
│                    │
│  [ 登录按钮 ]       │ ← 品牌蓝色，圆角8px
│                    │
└────────────────────┘

按钮规格:
- 宽度: 自适应内容
- 水平内边距: 24px
- 垂直内边距: 12px
- 背景: #3b82f6
- 文字: "登录", 16px, 600字重, 白色
```

#### 已登录态设计：

**用户头像区域**:
```
容器:
- 背景: 白色 #ffffff
- 内边距: 24px
- 对齐: 居中
- 底部分割线: 1px solid #e5e7eb

头像圆形:
- 尺寸: 80×80px
- 圆形（border-radius: 40px）
- 背景: #3b82f6（品牌蓝）
- 内容: 用户名首字母（大写）
- 文字: 32px, 粗体, 白色

用户名:
- 字号: 20px
- 字重: 600
- 颜色: #1f2937
- 上边距: 16px

邮箱:
- 字号: 14px
- 颜色: #6b7280
- 上边距: 4px
```

**统计数据区域**:
```
容器:
- 背景: 白色
- 方向: 水平排列（flex-direction: row）
- 内边距: 20px
- 上边距: 12px

单项统计:
- 宽度: 平分（flex: 1）
- 对齐: 居中

统计数值:
- 字号: 24px
- 字重: bold
- 颜色: #1f2937

统计标签:
- 字号: 12px
- 颜色: #6b7280
- 上边距: 4px

示例:
┌─────────────┬─────────────┐
│    1250     │     ✓       │
│    积分     │  认证状态    │
└─────────────┴─────────────┘
```

**菜单列表区域**:
```
容器:
- 背景: 白色
- 上边距: 12px

菜单项:
- 高度: 自适应
- 内边距: 16px（上下左右）
- 底部分割线: 1px solid #e5e7eb（最后一项无）

菜单文字:
- 字号: 16px
- 颜色: #1f2937

特殊项（退出登录）:
- 文字颜色: #ef4444（危险红）

菜单项列表:
1. 我的提交
2. 设置
3. 退出登录（红色）
```

---

### 模块五：登录/注册（Auth Screens）

**页面定位**: 用户身份验证入口
**设计原则**: 简洁、信任感强、低摩擦

#### 登录页（Login Screen）设计：

**整体布局**:
```
容器:
- 背景: #f9fafb
- 全屏垂直居中布局
- 水平内边距: 20px

内容结构（从上到下）:
┌──────────────────────────┐
│                          │
│      反内卷               │ ← 32px, 粗体, #1f2937
│  扫码识企业，共同抵制内卷   │ ← 14px, #6b7280, 下方40px
│                          │
│  ┌──────────────────┐   │
│  │ 用户名            │   │ ← 输入框
│  └──────────────────┘   │
│  ┌──────────────────┐   │
│  │ 密码              │   │ ← 密码输入框
│  └──────────────────┘   │
│                          │
│  ┌──────────────────┐   │
│  │      登录         │   │ ← 主按钮
│  └──────────────────┘   │
│                          │
│   没有账号？立即注册      │ ← 链接按钮
│                          │
└──────────────────────────┘
```

**表单元素规格**:
```
表单容器:
- 最大宽度: 320px
- 宽度: 100%

输入框:
- 背景: 白色 #ffffff
- 边框: 1px solid #e5e7eb
- 圆角: 12px
- 内边距: 16px
- 字号: 16px
- 下边距: 12px
- 占位符颜色: 继承系统默认

主按钮（登录）:
- 背景: #3b82f6（品牌蓝）
- 圆角: 12px
- 内边距: 16px
- 上边距: 8px
- 文字: "登录", 16px, 600字重, 白色
- 居中对齐

次级按钮（注册链接）:
- 背景: 透明
- 上边距: 16px
- 文字: "没有账号？立即注册", 14px, #3b82f6
- 居中对齐
```

**注册页（Register Screen）**:
- 布局风格与登录页保持一致
- 表单字段增加：确认密码、邮箱（可选）
- 主按钮文字改为"注册"
- 次级链接改为"已有账号？立即登录"

---

## 🔧 特殊组件设计规范

### 1. 认证徽章（CertificationBadge）

```
用途: 显示企业的认证等级
尺寸选项: small / medium / large

视觉特征:
- 形状: 圆角矩形或盾牌形状
- 包含图标: ✓ 或 shield-checkmark
- 配色: 金色/绿色主题
- 文字: 认证等级（如"金牌认证"、"银牌认证"）
```

### 2. 认证状态条（CertificationStatus）

```
用途: 显示认证审核进度
状态类型:
- none: 未申请（不显示或显示"未认证"）
- pending: 待审核（黄色进度条）
- under_review: 审核中（蓝色动画进度条）
- approved: 已通过（绿色完成标记）
- rejected: 未通过（红色拒绝标记）

视觉呈现:
- 进度条形式或状态标签形式
- 配合文字说明
```

### 3. 代工标签（OEM Badge）

```
用途: 标识代工生产的产品
视觉样式:
- 背景: #fef3c7（浅黄）
- 文字: #92400e（深橙棕）
- 内边距: 8px（垂直）× 12px（水平）
- 圆角: 8px
- 文字: "代工产品", 12px, 500字重
- 居中显示
```

### 4. 条形码扫描器（BarcodeScanner）

```
界面元素:
- 全屏黑色背景相机预览
- 四角扫描框（蓝色半透明，带动画）
- 顶部状态栏区域（时间、信号等）
- 底部提示文字区域
- 手电筒切换按钮（右上角）
- 相册选择按钮（右下角，可选）

动画效果:
- 扫描线: 从上到下的水平线往返运动
- 四角框: 呼吸灯效果（透明度变化）
- 识别成功: 绿色闪烁 + 震动反馈
```

---

## 📐 导航与布局系统

### 底部标签栏（Tab Bar）

```
标签数量: 3个
标签项:
1. 扫描（scan）- 首页默认选中
   图标: scan (Ionicons)
   
2. 搜索（search）
   图标: search (Ionicons)
   
3. 我的（profile）
   图标: person (Ionicons)

样式规范:
- 高亮颜色: #3b82f6（品牌蓝）
- 非高亮颜色: #6b7280（灰色）
- 背景色: #ffffff（白色）
- 顶部边框: 1px solid #e5e7eb
- 高度: 符合平台规范（iOS安全区域适配）
- 图标尺寸: 24px（默认）
- 文字字号: 10-12px
- 选中态: 图标+文字均为品牌蓝
```

### 页面导航栏（Header）

```
样式规范:
- 背景色: #ffffff
- 标题字重: 600
- 标题颜色: #1f2937
- 返回按钮: 左侧箭头图标（<）
- 高度: 符合平台导航栏标准

特殊处理:
- 扫描页: 隐藏导航栏（沉浸式体验）
- 详情页: 显示返回按钮+标题
- 搜索页: 仅显示搜索框（自定义header）
```

### 页面转场动画

```
推送新页面（Push）:
- 方向: 从右侧滑入
- 时长: 300ms
- 缓动: ease-out

返回上一页（Pop）:
- 方向: 向右侧滑出
- 时长: 250ms
- 缓动: ease-in

模态弹出（Modal）:
- 方向: 从底部滑入
- 背景: 半透明遮罩
- 支持下滑关闭

标签切换（Tab Switch）:
- 无明显动画或淡入淡出
- 时长: 200ms
```

---

## 📱 响应式设计要求

### 屏幕尺寸适配

**设计基准**:
- 主设计尺寸: iPhone 14 (390×844pt)
- 最小支持: iPhone SE (375×667pt)
- 最大支持: iPhone 15 Pro Max (430×932pt)
- Android: 360-412dp 宽度范围

**适配策略**:
- 使用相对单位（flex、百分比）
- 关键数值使用屏幕宽度的比例计算
- 文字大小在±2px范围内微调
- 间距按比例缩放（最小12px，最大24px）

### 安全区域处理

```
iOS:
- 顶部: 适配刘海/Dynamic Island（status bar height）
- 底部: 适配Home Indicator（safe area bottom）

Android:
- 顶部: 适配status bar
- 底部: 适配navigation bar gesture handle

实现方式:
- react-native-safe-area-context
- 所有页面容器包裹 SafeAreaView
- TabBar自动处理底部安全区域
```

### 横屏模式

```
支持情况: 不推荐横屏（锁定竖屏）
如需支持:
- 扫描界面: 保持全屏相机预览
- 表格类数据: 可考虑横向展示
- 卡片布局: 调整为多列网格
```

---

## ♿ 可访问性（Accessibility）设计

### 色彩对比度

```
正文文字（AA级标准）:
- 白色背景上的文字: 对比度 ≥ 4.5:1
  ✅ #1f2937 on #fff (对比度 ~12:1)
  ✅ #3b82f6 on #fff (对比度 ~4.5:1)
  ⚠️ #6b7280 on #fff (对比度 ~4.5:1 - 勉强达标)

大文字（AAA级标准，≥18px或14px粗体）:
- 对比度 ≥ 3:1
  ✅ 所有标题均满足

徽章内文字（白色 on 彩色背景）:
- 需确保彩色背景亮度足够
- 建议增加文字阴影或加深背景
```

### 触控目标尺寸

```
最小触控区域: 44×44pt（Apple HIG标准）
推荐尺寸: 48×48pt

应用场景:
- 按钮: 最小高度 48px
- 列表项: 最小高度 56px
- 图标按钮: 48×48pt 的触控热区（图标可较小）
- 链接文字: 增加 padding 达到 44px 高度
```

### 屏幕阅读器支持

```
必要属性:
- 每个可交互元素添加 accessibilityLabel
- 图标按钮添加描述性标签（如"返回"而非仅箭头图标）
- AGI徽章: 读出分数和等级（如"反内卷指数75分，内卷严重"）
- 状态变化: 使用 accessibilityState 通知
- 图片: 添加 accessibilityHint 或隐藏装饰性图片

语义化结构:
- 使用正确的组件角色（button、link、image等）
- 合理的阅读顺序（tab order）
- 重要信息不依赖颜色单独传达
```

---

## 🎭 微交互与动效设计

### 触觉反馈（Haptics）

```
AGI徽章交互:
- 红色徽章（内卷严重）→ Warning 震动模式
- 绿色徽章（员工友好）→ Success 震动模式

操作反馈:
- 按钮点击 → Light Impact
- 扫描成功 → Success 震动
- 错误/警告 → Error 或 Warning 震动
- 数据刷新 → Medium Impact
```

### 加载动画

```
全局加载:
- ActivityIndicator（圆形旋转）
- 颜色: #3b82f6
- 位置: 居中或内容区上方

下拉刷新:
- 平台原生 RefreshControl
- 颜色: #3b82f6

骨架屏（可选增强）:
- 扫描结果卡片骨架
- 公司列表项骨架
- 动画: shimmer 效果（脉冲式闪烁）
```

### 状态转换动画

```
按钮状态:
- 默认 → 按下: 缩放至 0.97，透明度降至 0.9
- 时长: 100ms
- 缓动: ease-in-out

卡片点击:
- 默认 → 按下: 轻微缩放 + 阴影减弱
- 时长: 150ms

列表项出现:
- 淡入 + 从下方轻微上移
- 时长: 300ms
- 交错延迟: 50ms per item

页面切换:
- 见上文导航动画规范
```

---

## 🌙 深色模式支持（可选预留）

> 注：当前版本以浅色模式为主，但建议预留深色模式变量以便未来扩展

```
色彩映射关系:

浅色模式 → 深色模式
- 页面背景 #f9fafb → #111827
- 卡片背景 #ffffff → #1f2937
- 主要文字 #1f2937 → #f9fafb
- 次要文字 #4b5563 → #9ca3af
- 辅助文字 #6b7280 → #6b7280
- 边框 #e5e7eb → #374151
- 输入框背景 #f3f4f6 → #374151

保持不变的颜色:
- 品牌蓝 #3b82f6
- 语义色（绿/黄/红）
- AGI徽章渐变色
```

---

## 📦 Figma 设计交付物清单

### 必需的设计稿

#### 1. 核心页面（High-Fidelity Mockups）
- [ ] **启动页/闪屏**（Splash Screen）
  - Logo展示 + 品牌标语
  - 加载动画

- [ ] **登录页**（Login Screen）
  - 完整表单布局
  - 输入框各状态（默认/聚焦/错误）
  - 按钮状态（默认/按下/禁用）

- [ ] **注册页**（Register Screen）
  - 同登录页风格的扩展表单

- [ ] **首页-扫描界面**（Scan Screen - Camera View）
  - 全屏相机取景框
  - 扫描框动画示意
  - 操作按钮布局

- [ ] **首页-扫描结果**（Scan Screen - Result Card）
  - 结果卡片完整布局
  - 各信息区块
  - AGI徽章展示

- [ ] **搜索页**（Search Screen）
  - 搜索栏
  - 结果列表（多种状态）
  - 空状态

- [ ] **公司详情页**（Company Detail Screen）
  - 完整滚动布局
  - 各信息区块
  - 操作按钮区
  - 加载/错误状态

- [ ] **个人中心-未登录**（Profile - Guest State）
  - 登录引导

- [ ] **个人中心-已登录**（Profile - Logged In）
  - 完整用户信息
  - 统计数据
  - 菜单列表

#### 2. 组件库（Component Library）
- [ ] **AGI评分徽章**
  - 4种状态 × 3种尺寸 = 12个变体
  - 減去未知状态的小尺寸 = 11个实际组件

- [ ] **认证徽章**（CertificationBadge）
  - 多个等级变体

- [ ] **认证状态条**（CertificationStatus）
  - 5种状态变体

- [ ] **按钮系统**
  - 主按钮（Primary Button）
  - 次级按钮（Secondary Button）
  - 文字按钮（Text Button）
  - 危险按钮（Danger Button）
  - 各按钮的状态变体（default/hover/pressed/disabled）

- [ ] **输入框**（Text Input）
  - 默认状态
  - 聚焦状态
  - 错误状态
  - 禁用状态
  - 带图标的输入框

- [ ] **卡片组件**
  - 扫描结果卡片
  - 公司列表项卡片
  - 信息展示卡片
  - 数据记录卡片

- [ ] **标签/徽章**（Tags & Badges）
  - 代工标签
  - 验证状态标签
  - 自定义标签

- [ ] **导航组件**
  - 底部标签栏（Tab Bar）
  - 顶部导航栏（Navigation Header）
  - 返回按钮
  - 搜索栏

- [ ] **加载状态**
  - 全屏加载
  - 列表骨架屏
  - 按钮加载状态

- [ ] **空状态**（Empty States）
  - 搜索无结果
  - 数据为空
  - 网络错误

#### 3. 图标集（Icon Set）
- [ ] 底部导航图标（3个）
- [ ] 功能性图标（搜索、扫描、设置、返回等）
- [ ] 状态图标（认证、警告、成功、错误等）
- [ ] 品牌图标（Logo、Logo Mark）

#### 4. 资源文件（Assets）
- [ ] Logo（SVG/PNG，多种尺寸）
- [ ] 启动图（Launch Image）
- [ ] App Icon（各平台所需尺寸）
- [ ] 默认头像占位图
- [ ] 渐变色定义文件

### 可选但推荐的设计稿

- [ ] **引导页/Onboarding**（首次使用引导，2-3页）
- [ ] **提交工时表单页**（Report Work Hours Form）
- [ ] **企业认证申请页**（Certification Application）
- [ ] **我的提交列表页**（My Submissions List）
- [ ] **设置页**（Settings Page）
- [ ] **消息通知页**（Notifications）
- [ ] **社区/排行榜页**（Community Leaderboard）
- [ ] **深色模式版本**（Dark Mode Versions）
- [ ] **Android Material Design适配版**（如需差异化设计）

---

## 🎯 设计原则总结

### 核心设计价值观

1. **透明可信**
   - 数据来源清晰可见
   - 验证机制可视化
   - 认证标识醒目但不突兀

2. **信息高效**
   - AGI评分一目了然（颜色+数字双重编码）
   - 信息层级分明（字号、字重、颜色三级区分）
   - 减少认知负荷（渐进式信息披露）

3. **行动导向**
   - 扫描即得结果（最少步骤）
   - 关键操作按钮显眼（提交、认证申请）
   - 社区激励机制明确（积分、勋章）

4. **情感共鸣**
   - 用色彩传递态度（红=警惕，绿=安心）
   - 触觉反馈增强感知
   - 语言贴近用户（"反内卷"、"避雷"）

5. **包容友好**
   - 符合无障碍标准
   - 支持多尺寸设备
   - 预留深色模式扩展能力

### 设计禁忌

❌ 避免过度使用红色（造成焦虑疲劳）
❌ 避免信息过载（单屏不超过3个核心信息点）
❌ 避免复杂手势操作（优先明确按钮）
❌ 避免纯装饰性元素（每个元素都有功能目的）
❌ 避免使用难以辨识的自定义图标（优先使用标准图标库）

---

## 📝 补充说明与技术约束

### 技术栈限制

```
框架: React Native (Expo SDK 50)
路由: Expo Router (File-based routing)
状态管理: Zustand
网络请求: Axios + TanStack React Query
动画: React Native Reanimated
手势: React Native Gesture Handler
矢量图形: React Native SVG
线性渐变: Expo Linear Gradient
触觉反馈: Expo Haptics
相机/扫码: Expo Camera + Expo Barcode Scanner
图标: Ionicons (@expo/vector-icons)
```

### 性能考虑

```
图片资源:
- 优先使用 SVG（可缩放、体积小）
- PNG 图片提供 @1x/@2x/@3x 三套
- 使用 WebP 格式优化体积

动画性能:
- 使用 Reanimated（原生线程驱动）
- 避免 JS 线程频繁操作
- 列表使用 FlatList/VirtualizedList

字体加载:
- 使用系统字体（无需额外加载）
- 如需自定义字体，使用 expo-font 预加载
```

### 国际化预留

```
虽然当前版本针对中文用户，但建议:
- 所有文案使用 i18n key 管理
- UI 布局支持 RTL（从右到左）语言
- 数字格式本地化（千分位、小数点）
- 日期格式本地化
- 文本长度预留 30%-50% 扩展空间（德语等长文本语言）
```

---

## ✅ 设计验收标准

### 视觉一致性检查清单
- [ ] 所有页面使用统一的色彩系统
- [ ] 字体层级符合规范（不超过6个层级）
- [ ] 间距使用 4px 基础网格
- [ ] 圆角值来自规定的集合（8/12/16/20/24/40px）
- [ ] 阴影效果统一（最多2种卡片阴影级别）
- [ ] 图标风格一致（全部线性/全部填充，不混用）

### 交互完整性检查清单
- [ ] 每个可点击元素有明确的触摸反馈
- [ ] 所有表单包含验证状态（错误提示）
- [ ] 加载状态覆盖所有异步操作
- [ ] 空状态设计覆盖所有可能场景
- [ ] 错误状态包含重试机制
- [ ] 导航路径清晰（用户知道如何返回）

### 可访问性检查清单
- [ ] 色彩对比度达到 AA 标准
- [ ] 所有触控目标 ≥ 44×44pt
- [ ] 支持屏幕阅读器（正确标注）
- [ ] 不依赖单一颜色传达信息
- [ ] 支持动态字体大小调整

### 开发交接检查清单
- [ ] 提供完整的设计规范文档（本文档）
- [ ] 导出组件库（Figma Components）
- [ ] 标注所有交互状态和动效参数
- [ ] 提供切图资源（正确的命名和格式）
- [ ] 包含响应式断点和适配说明
- [ ] 注明任何特殊的技术实现注意事项

---

## 🚀 后续迭代方向（Phase 2 预留）

基于当前 MVP 功能，未来可扩展的设计模块：

1. **社交功能增强**
   - 评论/点赞系统
   - 用户关注/粉丝
   - 动态Feed流

2. **数据可视化升级**
   - AGI趋势图表（折线图/面积图）
   - 行业对比雷达图
   - 个人消费习惯分析饼图

3. **游戏化元素**
   - 成就系统UI
   - 积分商城
   - 排行榜（日/周/月/总）

4. **AR增强体验**
   - AR实景扫描叠加信息
   - 3D公司名片展示

5. **智能推荐**
   - 个性化避雷推荐
   - 相似产品对比视图
   - 购物清单智能分析

---

## 📞 设计沟通与协作

### 设计评审要点

每次设计评审应关注：
1. **信息架构是否合理** - 用户能否快速找到关键信息？
2. **AGI评分是否突出** - 核心价值主张是否一眼可见？
3. **操作路径是否最短** - 从扫码到看到结果需要几步？
4. **情感设计是否到位** - 是否能引起用户共鸣和行动意愿？
5. **异常情况是否考虑** - 网络、权限、数据缺失等边缘case？

### 开发协作建议

- 使用 Figma Dev Mode 查看精确数值
- 建立 Design Token 共享（CSS Variables / Styled Components）
- 定期 Design-Dev Sync 会议（每周1次）
- 使用 Storybook 或类似工具进行组件验收测试
- 维护 UI Regression 测试截图对比

---

**文档版本**: v1.0
**最后更新**: 2026-04-19
**适用范围**: AntiGrind APP MVP 版本（v1.0.0）
**设计工具**: Figma（推荐使用 Auto Layout & Variants 功能）
